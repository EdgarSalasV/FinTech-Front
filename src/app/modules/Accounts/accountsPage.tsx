import * as React from "react";
import AccountCard from './accountsCard';
const AccountPage = () => {
  return (
    <React.Fragment>
      <AccountCard />
    </React.Fragment>
  );
};

export default AccountPage;
